SEL nombre_sucursal = "navacerrada" (prestamo)
SEL importe>1200 (prestamo)
SEL nombre_sucursal = "navacerrada" AND importe >1200(prestamo)
SEL departamento = 10 (empleado)
PRO nombre_cliente (prestatario)
PRO numero_prestamo,importe(prestamo)
PRO nombre_cliente(SEL ciudad_cliente="Peguerinos" (cliente) )
(PRO nombre_cliente(prestatario)) UNI (PRO nombre_cliente(impositor))
PRO nombre_cliente (impositor) DIF PRO nombre_cliente (prestatario)
SEL nombre_sucursal = "navacerrada" (prestatario PROC prestamo)
SEL p.numero_prestamo = a.numero_pretamo (SEL nombre_sucursal = "navacerrada" (prestatario p PROC prestamo a ))
PRO nombre_cliente ( SEL p.numero_prestamo = a.numero_prestamo ( SEL nombre_sucursal = "navacerrada" (prestatario p PROC prestamo a )))
PRO c.saldo*10 ( SEL c.saldo < d.saldo ( cuenta c PROC cuenta d))
(PRO saldo (cuenta)) DIF (PRO c.saldo ( SEL c.saldo < d.saldo ( cuenta c PROC cuenta d)))
PRO calle_cliente, ciudad_cliente (SEL nombre_cliente = "Gomez" (cliente))
PRO cliente.nombre_cliente ( cliente PROC PRO calle_cliente, ciudad_cliente (SEL nombre_cliente = "Gomez" (cliente)))
PRO nombre_cliente(prestatario) INT PRO nombre_cliente (impositor)
PRO nombre_cliente, numero_prestamo, importe (prestatario REUN prestamo)
(PRO nombre_cliente, nombre_sucursal (impositor REUN cuenta)) DIV (PRO nombre_sucursal ( SEL ciudad_sucursal = "Arganzuela" (sucursal)))
(PRO numero_cuenta, nombre_sucursal, saldo (SEL saldo > 10000 (cuenta))) UNI (PRO numero_cuenta, nombre_sucursal, saldo (SEL saldo <= 10000 (cuenta)))
SEL ((dno=4 AND salario > 25000) OR (ND = 5 AND salario > 30000)) (empleado)
PRO nombre, apellido, salario (SEL nd = 5 (EMPLEADO))
SEL nombre = "james" AND apellido = "Borg" (empleado)
PRO mat,cod (notas) DIV PRO cod (Asignatura)
SEL rut, vigencia(dueno) DIF SEL rut, vigencia (chofer)
SEL dueno.rut=movil.rut_dueno (dueno PROC movil)
SEL fecha_licencia_hasta < 18/7/1999 (chofer)
(PRO patente, rut_chofer (Movil)) DIV (PRO rut (SEL fecha_licencia_hasta < 18/7/1999 (chofer)))
PRO cedula, nombre, salario (SEL ((sexo = "M") OR (salario >=1350000)) (persona) )
PRO nombre (articulos PROC SEL p="p1" (envios))
PRO envios (envios PROC SEL color != "ROJO" (componentes))
PRO t (envios DIV PRO c(componentes))
SEL sueldo >= 50000 AND fecha_inicio >= 1/1/2003 (empleado)
SEL nombre="ana" (PRO nombre,apellido,fecha_nac (persona))
empleado PROC SEL nombre = soto (empleado)
empleado INT departamento
PRO title, studioName ( SEL length >= 100 (Movie) INT SEL studioName="Fox" (Movie))
PRO starName (SEL lenght>= 100 ( Movie REUN Movie_Star))
PRO proyecto.nombre (SEL (proyecto.fechainicio=2009) AND (plan.cod_plan = "peur") AND (plan.cod_plan = proyecto.cod_plan) (proyecto PROC plan))
PRO nombre_m (SEL cargo = "presidente" (composicion) PROC miembro)
PRO nombre (federacion) DIF PRO nombre (SEL cargo ="asesortecnico" (composicion))
PRO nombre (SEL cargo = "asesortecnico" (composicion)) INT PRO nombre (SEL cargo = "psicologo" (composicion))
PRO titulo_rev( revista PROC PRO referencia,codtema (articulo) DIV PRO codtema (tema))
PRO nombre (hardware PROC PRO h (hardware) DIF PRO h (instalacion) )
PRO pnombre (papeleria PROC suministro PROC SEL ciudad = "madrid" (editorial))
PRO titulo (libro PROC PRO l (libro) DIF PRO l (suministro))
PRO nom_cli, domicilio (SEL descuento >10 (venta) PROC cliente)
PRO nom_c, presupuesto ( PRO c (club) DIF PRO c (SEL puesto = 1 (participacion) PROC club) )
papeleria PROC PRO e,l,p (suministro) DIV PRO l,p (suministro)
libro PROC PRO l (libro) DIF PRO l (suministro)
PRO sal+300 (saldos)
PRO edad-1 (edades)
PRO interes*10-5 (intereses)
PRO sueldo/4-2*1+4 (sueldos)
PRO (sueldo*3),(edad+6),nombre (sueldos)
PRO nombre="lis",
apellido="eslava"
(alumno)